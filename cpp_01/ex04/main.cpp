#include <string>

int main ()
{
	std::string	s = "abc"; //s가 string 클래스 이걸 쓴다. 
	s.replace()    //x 맴버 함수 
	s.length()
	. ... //string 안에 있는 내자ㅏㅇ 함수를 쓴다.

	char   *str

	strlen(str);
}

// 내장함수를 이용해서 replace 같은 친구를 사용하는거임